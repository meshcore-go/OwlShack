{
  "topLevelCommands": [
    { "command": "ver", "hint": "firmware version + build date" },
    { "command": "board", "hint": "board manufacturer name" },
    { "command": "region", "hint": "show / set radio region" },
    { "command": "advert", "hint": "broadcast a flood advert now" },
    { "command": "advert.zerohop", "hint": "broadcast a zero-hop advert (neighbors only)" },
    { "command": "neighbors", "hint": "list observed neighbors" },
    { "command": "discover.neighbors", "hint": "request neighbours to advertise (~60s)" },
    { "command": "neighbor.remove ", "hint": "remove a neighbor by pubkey hex" },
    { "command": "clock", "hint": "show device UTC time" },
    { "command": "clock sync", "hint": "sync device clock to companion timestamp" },
    { "command": "time ", "hint": "set time to <epoch-seconds>" },
    { "command": "clkreboot", "hint": "reset clock to 2024-05-15 then reboot" },
    { "command": "tempradio ", "hint": "temporary radio params: freq bw sf cr mins" },
    { "command": "password ", "hint": "change admin password" },
    { "command": "clear stats", "hint": "reset packet/radio stat counters" },
    { "command": "stats-radio", "hint": "radio stats dump (serial only)" },
    { "command": "stats-packets", "hint": "packet stats dump (serial only)" },
    { "command": "stats-core", "hint": "core stats dump (serial only)" },
    { "command": "log start", "hint": "begin writing log to flash" },
    { "command": "log stop", "hint": "stop log writes" },
    { "command": "log erase", "hint": "erase log file" },
    { "command": "log", "hint": "dump log file (serial only)" },
    { "command": "powersaving on", "hint": "enable powersaving" },
    { "command": "powersaving off", "hint": "disable powersaving" },
    { "command": "powersaving", "hint": "show powersaving state" },
    { "command": "gps on", "hint": "enable GPS module" },
    { "command": "gps off", "hint": "disable GPS module" },
    { "command": "gps sync", "hint": "sync clock from GPS" },
    { "command": "gps setloc", "hint": "snap node lat/lon to current GPS fix" },
    { "command": "gps advert none", "hint": "do not include location in adverts" },
    { "command": "gps advert share", "hint": "advertise current GPS location" },
    { "command": "gps advert prefs", "hint": "advertise stored prefs lat/lon" },
    { "command": "gps", "hint": "show GPS state, fix and sat count" },
    { "command": "sensor list", "hint": "list sensor settings (paged)" },
    { "command": "sensor get ", "hint": "read a sensor setting by key" },
    { "command": "sensor set ", "hint": "set a sensor setting: key value" },
    { "command": "reboot", "hint": "soft reboot the device" },
    { "command": "poweroff", "hint": "power off (alias: shutdown)" },
    { "command": "shutdown", "hint": "power off (alias: poweroff)" },
    { "command": "start ota", "hint": "begin OTA firmware update" },
    { "command": "erase", "hint": "format the file system (serial only)" }
  ],
  "configKeys": [
    { "key": "name", "hint": "node name (set: max 31 chars, no special chars)" },
    { "key": "owner.info", "hint": "owner bio; use | as newline separator" },
    { "key": "role", "hint": "device role (read-only)" },
    { "key": "public.key", "hint": "this node's public key (read-only)" },
    { "key": "prv.key", "hint": "private key — serial only, reboot to apply" },
    { "key": "radio", "hint": "freq,bw,sf,cr — reboot to apply" },
    { "key": "freq", "hint": "frequency in MHz — serial only, reboot to apply" },
    { "key": "tx", "hint": "TX power in dBm" },
    { "key": "radio.rxgain", "hint": "RX boosted gain (on/off, SX126x/LR1110 only)" },
    { "key": "lat", "hint": "latitude (decimal degrees)" },
    { "key": "lon", "hint": "longitude (decimal degrees)" },
    { "key": "advert.interval", "hint": "direct advert interval — minutes (3-240, 0=off)" },
    { "key": "flood.advert.interval", "hint": "flood advert interval — hours (3-168, 0=off)" },
    { "key": "repeat", "hint": "packet forwarding (on/off)" },
    { "key": "flood.max", "hint": "max flood transmissions (0-64)" },
    { "key": "flood.max.unscoped", "hint": "max hops for un-scoped floods (0-64)" },
    { "key": "flood.max.advert", "hint": "max hops for advert floods (0-64)" },
    { "key": "path.hash.mode", "hint": "path hash size: 0=1B, 1=2B, 3=4B" },
    { "key": "loop.detect", "hint": "off / minimal / moderate / strict" },
    { "key": "rxdelay", "hint": "RX delay base, dB (0-20)" },
    { "key": "txdelay", "hint": "TX delay factor (0-2)" },
    { "key": "direct.txdelay", "hint": "direct-mode TX delay factor (0-2)" },
    { "key": "dutycycle", "hint": "duty cycle %, 1-100 (sets airtime_factor)" },
    { "key": "af", "hint": "airtime factor (raw)" },
    { "key": "int.thresh", "hint": "interference threshold" },
    { "key": "agc.reset.interval", "hint": "AGC reset interval, secs (rounded /4)" },
    { "key": "multi.acks", "hint": "number of repeated ACKs to send" },
    { "key": "guest.password", "hint": "guest login password" },
    { "key": "allow.read.only", "hint": "allow read-only mode (on/off)" },
    { "key": "bootloader.ver", "hint": "bootloader version (NRF52 only, read-only)" },
    { "key": "bridge.type", "hint": "bridge build (none/rs232/espnow, read-only)" },
    { "key": "bridge.enabled", "hint": "enable bridge (on/off)" },
    { "key": "bridge.delay", "hint": "bridge delay, ms (0-10000)" },
    { "key": "bridge.source", "hint": "bridge packet source (rx/tx)" },
    { "key": "bridge.baud", "hint": "RS232 bridge baud (9600+)" },
    { "key": "bridge.channel", "hint": "ESP-NOW channel (1-14)" },
    { "key": "bridge.secret", "hint": "ESP-NOW shared secret" },
    { "key": "adc.multiplier", "hint": "battery ADC multiplier (0=board default)" },
    { "key": "pwrmgt.support", "hint": "power management supported? (NRF52 only)" },
    { "key": "pwrmgt.source", "hint": "power source: external / battery" },
    { "key": "pwrmgt.bootreason", "hint": "last reset / shutdown reason" },
    { "key": "pwrmgt.bootmv", "hint": "boot voltage in mV" }
  ]
}
